info:
  name: PostToevoegenLijst
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8082/restservices/list/createShoppingList/Dum-Dum
  body:
    type: json
    data: |-
      {
        "name": "Boodschappenlijst"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

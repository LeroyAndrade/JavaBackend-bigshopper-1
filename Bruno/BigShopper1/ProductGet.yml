info:
  name: ProductGet
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8082/restservices/product
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

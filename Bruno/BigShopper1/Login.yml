info:
  name: Login
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8082/restservices/authentication
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "leroy",
        "password": "password123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

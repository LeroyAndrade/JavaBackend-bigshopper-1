info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8082/restservices/list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

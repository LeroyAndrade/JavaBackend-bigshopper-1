info:
  name: Patch customer product
  type: http
  seq: 9

http:
  method: PATCH
  url: http://localhost:8082/restservices/product/patchName/MyListGeslaagd
  body:
    type: json
    data: |-
      {
        "name" : "Rijst"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
